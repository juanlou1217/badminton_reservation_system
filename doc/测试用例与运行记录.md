# 测试用例与运行记录

## 自动化测试结果

```text
34 passed, 1 skipped
```

## 测试用例

| 编号 | 测试内容 | 输入示例 | 预期结果 |
| --- | --- | --- | --- |
| T01 | 用户注册 | bob / 13700000000 / secret123 | 注册成功，密码哈希保存，角色为 user |
| T02 | 管理员登录 | admin / admin123 | 登录成功，角色为 admin |
| T03 | 登录失败 | 正确用户名 / 错误密码 | 提示账号或密码错误 |
| T04 | 禁用用户登录 | disabled 用户 | 提示账号已被禁用 |
| T05 | 正常预约 | 可用时间段 | 生成预约编号，状态为 booked |
| T06 | 重复预约 | 已被预约时间段 | 预约失败，提示时间段已被预约 |
| T07 | 每日预约限制 | 超过 max_daily_reservations | 预约失败，提示超过每日预约次数限制 |
| T08 | 取消预约 | 本人有效预约 | 状态变为 cancelled，取消时间写入 |
| T09 | 越权取消 | 其他用户预约 | 取消失败，提示只能取消自己的预约 |
| T10 | 管理员权限 | 普通用户调用管理员功能 | 拒绝访问 |
| T11 | 场地新增 | C02 / 二号场 | 场地写入数据库 |
| T12 | 时间段生成 | 指定日期和时间范围 | 生成可预约时间段 |
| T13 | 统计汇总 | 多条预约记录 | 返回总数、有效数、取消数和场地统计 |
| T14 | Excel 导出 | 导出预约记录 | 生成可打开的 `.xlsx` 文件 |
| T15 | 管理员权限下沉 | 普通用户调用管理员服务 | 服务层拒绝访问 |
| T16 | 输入校验 | 非法手机号、短密码、非法场地状态 | 返回明确错误 |
| T17 | MySQL 集成检查 | `RUN_MYSQL_TESTS=1` | 远程数据库连接成功，项目表和关键预约约束存在 |
| T18 | ORM 与 SQL 结构一致性 | SQLAlchemy `create_all()` | `reservations.active_slot_id` 与唯一索引存在 |
| T19 | 统计/导出权限 | 普通用户调用统计或导出 | 服务层拒绝访问 |
| T20 | 时间段维护 | 管理员禁用可预约时间段 | 时间段状态更新为 disabled |
| T21 | Harness 自检 | `scripts/doctor.py` | 检查依赖、环境变量、关键文件，不泄露数据库密码 |
| T22 | 预约核销 | 管理员核销 booked 预约 | 预约状态变为 finished，界面显示已核销 |
| T23 | 核销权限 | 普通用户调用核销 | 服务层拒绝访问 |
| T24 | 核销后取消 | 管理员取消已核销预约 | 拒绝取消，避免状态回退 |
| T25 | 核销后每日限制 | 已核销预约 + 同日再次预约 | 仍计入每日预约次数限制 |

## 当前本地运行记录

```bash
.venv/bin/pytest -q
```

```text
34 passed, 1 skipped
```

远程 MySQL 集成测试需要本机 `.env` 或环境变量提供 `DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME`，并显式开启：

```bash
RUN_MYSQL_TESTS=1 .venv/bin/pytest tests/test_mysql_connection_optional.py -q
```

## 远程 MySQL 实测记录

```bash
.venv/bin/python scripts/check_db_connection.py
```

```text
数据库连接正常: database=juanlou, version=8.0.46, schema=关键约束正常
```

```bash
RUN_MYSQL_TESTS=1 .venv/bin/pytest tests/test_mysql_connection_optional.py -q
```

```text
1 passed
```
